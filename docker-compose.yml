services:
  peach-db:
    image: postgres:18-alpine
    container_name: peach-db
    platform: linux/arm64  # Optional: for Apple Silicon hosts
    env_file:
      - .env
    volumes:
      - peach-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  peach-data:
